import { TClassName } from '@/shared/types'
import { FC } from 'react'

interface Props extends TClassName {}
const SomeEntity: FC<Props> = ({ className }) => {
	return <></>
}

export { SomeEntity }
