import { TClassName } from '@/shared/types'
import { FC } from 'react'

interface Props extends TClassName {}
const SomeFeature: FC<Props> = ({ className }) => {
	return <></>
}

export { SomeFeature }
